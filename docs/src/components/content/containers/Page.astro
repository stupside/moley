---
import type { PageBlock } from "../../../types/content.js";
import { mergeClassName } from "../../../utils/classes.js";
import ContentRenderer from "../../ContentRenderer.astro";

export interface Props {
	content: PageBlock;
}

const { content } = Astro.props;
---

<div class={mergeClassName("flex flex-col gap-6", content.className)} id={content.id}>
  {content.children?.map(child => (
    <ContentRenderer content={child} />
  ))}
</div>