ingress:
    apps:
        - expose:
            subdomain: test
          target:
            hostname: localhost
            port: 3000
            protocol: http
    mode: subdomain
    zone: xonery.dev
tunnel:
    name: demo
    persistent: false
