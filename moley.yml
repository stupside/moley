ingress:
    apps:
        - expose:
            subdomain: test
          target:
            hostname: localhost
            port: 3000
            protocol: http
          access:
            session: "24h"
            providers:
              - github
              - onetimepin
            policy:
              decision: allow
              emails:
                - kilian@xonery.dev
    mode: subdomain
    zone: xonery.dev
tunnel:
    id: demo
    persistent: false
