# Moly Configuration
# This file contains the configuration for Moly tunnel management

# Cloudflare configuration
cloudflare:
  api_token: ""  # Your Cloudflare API token

# Zone configuration
zone: xonery.dev

# Applications to expose
apps:
  - port: 3000
    subdomain: local-api
  - port: 8080
    subdomain: local-web